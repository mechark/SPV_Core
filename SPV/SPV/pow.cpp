#include <iostream>
#include <vector>
#include <string>

#include "SHA256.h"
#include "spv.h"

namespace header_chain
{
	bool pow::proof_of_work(std::vector<models::header> headers)
	{
		bool is_correct = false;
		
		

		return is_correct;
	}
}